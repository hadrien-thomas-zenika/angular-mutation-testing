# Installation

```
npm install -D stryker-cli
```

```
npx stryker init
```

Go to the root directory to edit the stryker configuration file (in our case: *stryker.conf.json*) and set `"coverageAnalysis": "off"`.

# Run

```
npx stryker run
```
